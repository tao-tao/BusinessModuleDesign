Ec.screenSize = function() {
   	return {
       width: screen.width,
       height: screen.height,
       availWidth: screen.availWidth,
       availHeight: screen.availHeight
   	}
}

var screenHeight = Ec.screenSize().height;
var screenWidth = Ec.screenSize().width ;

var mainWindowHeight = screenHeight * 0.856 * 0.41;
var mainWindowWidth = screenWidth * 0.8;

var adminMainHeight = Ec.screenSize().height * 0.777;
var adminMainWidth = Ec.screenSize().width * 0.673;

var adminListHeight = Ec.screenSize().height*0.7;
var adminListWidth = Ec.screenSize().width * 0.673;

var adminFormHeight = Ec.screenSize().height*0.735;
var adminFormWidth = Ec.screenSize().width * 0.673;

var adminTabHeight = Ec.screenSize().height*0.665;
var adminTabWidth = Ec.screenSize().width*0.667;


var TreeHeight = Ec.screenSize().height * 0.745;
var TreeWidth = Ec.screenSize().width*0.15;

var FormHeight = Ec.screenSize().height*0.735;
var FormWidth = Ec.screenSize().width*0.72;

var TabHeight = Ec.screenSize().height*0.669;
var TabWidth = Ec.screenSize().width*0.738;

var FieldWidth = Ec.screenSize().width*0.15;
var TestAreaWidth = Ec.screenSize().width*0.53;
var LabelWidth = Ec.screenSize().width*0.11;
var otherWidth = Ec.screenSize().width*0.7;


var PageHeight = Ec.screenSize().height*0.8;
var PageWidth = Ec.screenSize().width*0.8;

var GridHeight = Ec.screenSize().height*0.6;
var GridWidth = Ec.screenSize().width*0.72;


var SelTreeHeight = Ec.screenSize().height*0.75;
var SelTreeWidth = Ec.screenSize().width*0.12;


var ListHeight = Ec.screenSize().height*0.22;
var ListWidth = Ec.screenSize().width*0.835;

var FormTabHeight = Ec.screenSize().height*0.5;
var FormTabWidth = Ec.screenSize().width*0.71;

var FormBodyHeight = Ec.screenSize().height*0.42;
var FormBodyWidth = Ec.screenSize().width*0.7;

var ScreenHeight = Ec.screenSize().height;
var ScreenWidth = Ec.screenSize().width;

var labelWidth = 120;
var fieldWidth = 150;
var shortFieldWidth = 93;
var shortCcyWidth = 60;



var mainWindowH = mainWindowHeight;
var mainWindowW = mainWindowWidth;